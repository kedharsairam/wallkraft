<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.WallKraft" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Branded launch splash (Android 12+ renders this natively via the
         SplashScreen API). After it finishes the activity drops into the
         normal app theme above. -->
    <style name="Theme.WallKraft.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.WallKraft</item>
    </style>
</resources>
